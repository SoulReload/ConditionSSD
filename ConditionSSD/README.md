# 💾 Корпоративный мониторинг здоровья дисков (SSD/HDD)

Этот Python-скрипт предназначен для автоматической проверки S.M.A.R.T. статуса дисков (особенно актуально для NVMe/SSD, чтобы определить износ в процентах) и отправки уведомления администратору по электронной почте, если здоровье диска падает ниже установленного порога (например, 60%).

## ⚙️ Установка

### Требования
1.  **Python 3.x**
2.  **smartmontools:** Скрипт использует системную утилиту `smartctl`. Она должна быть установлена и доступна в PATH.
* **Установка в Debian/Ubuntu:** `sudo apt update && sudo apt install smartmontools`
3.  **Права Администратора:** Скрипт должен запускаться с наивысшими правами для доступа к данным дисков.

### Конфигурация и безопасность
Для безопасного запуска скрипт требует установки следующих системных переменных среды:
* `DISK_MONITOR_USER`: Логин бота/почтового аккаунта для отправки.
* `DISK_MONITOR_PASS`: Пароль приложения для этого аккаунта.(если включена 2FA).

## ▶️ Развертывание и использование

Скрипт предназначен для автоматического запуска в корпоративной среде с помощью планировщика.

### 1. Автоматизация
Разверните и запустите скрипт через **Планировщик заданий (Windows Task Scheduler)** или **Cron (Linux)** с нужной периодичностью (рекомендуется — **каждые 1–4 часа**). Убедитесь, что задача запускается с **наивысшими правами**.
### 2. Развертывание в Linux (Cron)
Поскольку `smartctl` требует прав root, используется скрипт-обертка и файл с переменными:

**A. Создайте файл переменных (безопасность):**
В папке проекта создайте файл **`.env_vars`** (добавлен в `.gitignore`), содержащий ваши учетные данные:
```bash
export DISK_MONITOR_USER='ваш_логин@smtp.com'
export DISK_MONITOR_PASS='ваш_пароль_приложения'

### 2. Диагностика
* Все ошибки и сведения о работе записываются в файл **`disk_monitor.log`**.
* Если при запуске возникает ошибка **`Диски не найдены`**, это почти всегда означает, что скрипт был запущен без прав **Администратора/Системы**.
B. Создайте скрипт-обертку (run.sh): Скрипт загружает переменные, а затем запускает Python-код:
#!/bin/bash
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source "$SCRIPT_DIR/.env_vars"
/usr/bin/python3 "$SCRIPT_DIR/ConditionSSD.py"

Сделайте его исполняемым: chmod +x run.sh

C. Настройка Cron: Откройте crontab для root-пользователя и добавьте задание (пример: запуск каждый час):
sudo crontab -e
# Добавьте строку:
0 * * * * /path/to/your/script/run.sh